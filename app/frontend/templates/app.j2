<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Application</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    <div class="d-flex">
      <div class="bg-dark text-white" id="sidebar" style="width: 250px; height: 100vh; padding: 10px; border-right: 1px solid #444;">
        <h3 class="text-center">Menu</h3>
            
        <a href="http://{{backip}}:80/" class="btn btn-dark w-100 text-start mb-2 mt-3">Monitoring</a>
            
            
        <div class="accordion mb-2" id="accordionExample">
          <div class="accordion-item bg-dark border-0">
            <h2 class="accordion-header" id="headingOne">
              <button class="accordion-button text-white bg-dark collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="false" aria-controls="collapseOne">
                Architecture
              </button>
            </h2>
            <div id="collapseOne" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
              <div class="accordion-body bg-dark">
                <a href="http://{{backip}}:80/edge" class="nav-link btn btn-dark text-white">Edge</a>
                <a href="http://{{backip}}:80/iot" class="nav-link btn btn-dark text-white">IoT</a>
              </div>
            </div>
          </div>
        </div>

        <a href="http://{{backip}}:80/app" class="btn btn-dark w-100 text-start mb-2 active">Créer une application</a>
      </div>
      <div class="container-fluid">
        
        <h1 class="d-flex justify-content-center">Applications</h1>
  
        {% if err %}
        <div class="alert alert-danger" role="alert">
          {{err}}
        </div>
        {% endif %}
  
        <div class="d-flex justify-content-end m-4">
          <button type="button" class=" btn btn-primary" data-bs-toggle="modal" data-bs-target="#modal">
            Créer une application
          </button>
        </div>
          
        <div class="m-4">
          <table class="table table-striped">
            <thead>
              <tr>
                <th scope="col">Nom</th>
                <th scope="col">Actions</th>
              </tr>
            </thead>
            <tbody>
              {% for nom,manifest in apps %}
              <tr>
                <td>{{nom}}</td>
                <td>
                  <button type="button" class=" btn btn-warning" data-bs-toggle="modal" data-bs-target="#modalmodif{{nom}}">
                    Modifier
                  </button>
                
                  <button type="button" class=" btn btn-danger" data-bs-toggle="modal" data-bs-target="#modalsuppr{{nom}}">
                    Supprimer
                  </button>
                </td>
              </tr>
              
              <div class="modal fade " id="modalsuppr{{nom}}">
                <div class="modal-dialog">
                  <div class="modal-content">
                    <div class="modal-header">
                      <h5 class="modal-title" id="staticBackdropLabel">Supprimer l'application {{nom}} ?</h5>
                      <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body justify-content-center">
                      <form class="form" action="http://{{backip}}:80/delapp" method="post">
                        <input type="hidden" name="nom" value="{{nom}}">
    
                        <button type="submit" class="btn btn-dark">Supprimer</button>
                      </form> 
                    </div>
                  </div>
                </div>
              </div>
              <div class="modal fade " id="modalmodif{{nom}}">
                <div class="modal-dialog">
                  <div class="modal-content">
                    <div class="modal-header">
                      <h5 class="modal-title" id="staticBackdropLabel">Modifier l'application {{nom}} ?</h5>
                      <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body justify-content-center">
                      <form class="form" action="http://{{backip}}:80/modifapp" method="post">
                        <div class="mb-3">
                          <label for="ram" class="form-label">Manifest de l'application :</label>
                          <input type="hidden" value="{{nom}}" name="nom"/>
                          <textarea class="form-control" id="man" placeholder="Manifest" name="manifest">{{manifest}}</textarea>
                        </div>
                        <button type="submit" class="btn btn-warning">Modifier</button>
                      </form> 
                    </div>
                  </div>
                </div>
              </div>
              {% endfor %}
            </tbody>
          </table>
        </div>
  
        <div class="modal fade " id="modal">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="staticBackdropLabel">Créer une application</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>

              <div class="modal-body justify-content-center">

                <form class="form" action="http://{{backip}}:80/createapp" method="post">
                  <div class="mb-3 mt-3">
                    <label for="nom" class="form-label">Nom :</label>
                    <input type="nom" class="form-control" id="nom" placeholder="Entrer le nom de la VM" name="nom">
                  </div>
                  <div class="mb-3">
                    <label for="manifest" class="form-label">Manifest de l'application :</label>
                    <textarea class="form-control" id="manifest" placeholder="Manifest" name="manifest"></textarea>
                  </div>
                  <button type="submit" class="btn btn-dark">Ajouter</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>